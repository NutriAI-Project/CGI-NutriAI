apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: nutriai-mesh
  labels:
    app: api-gateway-service
spec:
  selector:
    app: api-gateway-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
